// Campbell Crowley (web@campbellcrowley.com)
// April 2021
//
/**
 * Power states.
 * @readonly
 * @enum {number}
 */
const PowerState = {
  /** On. */
  ON: 1,
  /** Off. */
  OFF: 0,
  /** We don't know for some reason. */
  UNKNOWN: -1,
};

module.exports = PowerState;
